// Public API for other features: they import from here, never from students' folders
// (BLUEPRINT.md, dependency rules).
export { useClassOptions } from './hooks/useClassOptions'
export type { ClassOption } from './types/student.types'
