// `theme` and `colorTheme` are also read by the inline script in index.html; change both together.
export const STORAGE_KEYS = {
  theme: 'erp-theme',
  colorTheme: 'erp-color-theme',
  sidebarCollapsed: 'erp-sidebar-collapsed',
  studentHiddenColumns: 'erp-student-hidden-columns',
} as const
